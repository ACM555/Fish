# AGENTS.md —— 水中机器鱼 · 科目一「障碍竞速」

本文件是本项目的工作约定与事实基准。任何人（含 AI 助手）在本目录下改动代码前，
先读本文件；本文件与代码冲突时，以「代码 + 官方规则」为准，并把冲突写回本文件。

---

## 1. 项目概况

| 项目 | 内容 |
| --- | --- |
| 赛事 | 先进机器人及仿真技术大赛 · 水中机器鱼仿真组 |
| 科目 | 科目一：障碍竞速（排位赛） |
| 队伍 ID | `F05012589`（见 `test.py` 的 `TEAM_NAME`） |
| 场地 | 水池 3000(x) × 2000(y) × 1500(z) mm，水底中心为原点 (0,0,0) |
| 障碍 | 4 根 200×200×1500 mm 立柱 |
| 机器鱼 | 1 条；鱼尾可控、鱼身被动、左右胸鳍推进器 |
| 计时 | 60 秒上限，超时记比赛失败；按用时升序排名 |
| 语言 | Python 3.7（软件包内嵌，无需自行安装） |
| 通信 | DDS（CycloneDDS）+ 官方 `cue` 库，发布/订阅 |

目录结构（实际）：

```
FishSim.V1/
├── test.py                                  ← 唯一策略文件（本项目全部逻辑）
├── AGENTS.md                                ← 本文件
├── .gitignore                               ← 已排除 WindowsNoEditor/
├── 水下机器鱼仿真软件使用手册-090701.docx      ← 官方使用手册
└── WindowsNoEditor/                         ← 仿真器本体（755MB，不纳入 Git）
    ├── FishSim.exe
    ├── Fish427/Binaries/Win64/
    │   ├── python.exe                       ← 内嵌 Python 3.7.7
    │   ├── cue.py                           ← 通信封装（publisher/subscriber/init）
    │   └── FishProjectMsg.py                ← DDS 数据结构定义
    └── Scripts/
        ├── project1.csv                     ← 官方成绩记录（裁判写入）
        ├── project1/                        ← 科目一策略放置目录（当前为空）
        └── project2/team1/main.py           ← 科目二示例
```

> `WindowsNoEditor/` 已在 `.gitignore` 中排除：含引擎二进制与资源包，单文件最大 99MB，
> 远超 GitHub 限制。需要托管请走 Release 附件或 Git LFS，不要直接提交。

---

## 2. 比赛规则（硬约束，不可违背）

**流程（必须按顺序，不能抄捷径）**

1. **绕位置 1 障碍转 1 圈** —— 从出发点 `(-1250, 0, 340)` 出发，先绕位置 1 的立柱
   `(-750, 0)` 完整转 **1 圈**，一圈都不能少。
2. **S 形穿过中场** —— 走「S」形路线穿过中场，从位置 2 附近经过，不能撞柱子。
3. **绕位置 3、4 之间转 2 圈** —— 到后半场，绕位置 3 与位置 4 之间的区域连续转 **2 圈**，
   这是最花时间的一段。
4. **到达终点** —— 全速冲向对面边线中点 `(1250, 0, 340)`，计时停止。

**平台与执行器限制**

| 部位 | 约束 |
| --- | --- |
| 鱼尾（尾部前-后关节） | ±80°，可控，模拟舵机 |
| 鱼身（身-尾前部） | ±10°，**被动**，无动力控制 |
| 胸鳍推进器 | 左右各一个，**推力上限 50**，角度沿 y 轴自由旋转 |

**关键物理（来自官方使用手册）**

- 鱼尾摆动是游动与转向的**主要动力方式**，**摆动频率与动力成正比**。
- 水下阻尼：横向移动 0.5，绕自身 x/z 轴翻转 0.5，其他方向 0.9。
- 平台以 **60 帧/秒** 运行仿真循环。

> 结论性推论（本项目提速的依据）：胸鳍推力已被规则封顶在 50，
> 想更快只能靠**尾摆频率/幅值**与**更短更顺的路径**，而不是继续加大推力。

---

## 3. 场地坐标与几何

```
起点 (-1250, 0, 340) ──→ 终点 (1250, 0, 340)      x 轴指向终点，单位 mm
```

| 名称 | 坐标 | 代码常量 |
| --- | --- | --- |
| 起点 | (-1250, 0) | `START` |
| 终点 | (1250, 0) | `GOAL` |
| 障碍 1 | (-750, 0) | `OBS1` |
| 障碍 2（中场） | (0, ±175) | `PILLARS` 中间两根 |
| 障碍 3 | (750, 0) | `OBS3` |

`test.py` 当前的立柱模型（`PILLARS`）：

```python
PILLARS = (OBS1, (0.0, -175.0), (0.0, 175.0), OBS3)
PILLAR_HALF_DIAG = 141.5      # 200×200 立柱的半对角线
```

即：**中场是一对间距 150mm 的立柱**（净空 y ∈ (-75, 75)，需要「穿缝」），
**后方 x=750 处是一根单柱**（绕它转 2 圈，圆心 (750,0)）。
绕行半径 `LOOP_R`：快档 200，`--stable` 档 210。

> ⚠️ **与官方教学页存在差异，见第 8 节「待确认事项」。**

---

## 4. 官方参考架构 vs 本项目实现

官方示范代码把策略拆成 4 个 `.py`（提交官网时也是 4 个文件）：

| 文件 | 职责 | 输入 → 输出 |
| --- | --- | --- |
| `path_generate.py` | ① 规划路径：赛前用 Dubins 曲线把绕桩路线算成几百个离散路径点 | → 路径点列表 `(x, y, 340, cos, sin, 0)` |
| `find_point.py` | ② 找目标点：每帧从路径点里挑出「下一个该去的点」，删掉已路过的 | → 目标点 `(target_x, target_y)` |
| `control.py` | ③ 算控制量：按「当前朝向 vs 目标方向」的误差算尾舵角与左右推力 | → 尾舵角、左推力、右推力 |
| `main.py` | ④ 主循环收发：每秒 60 次「收状态 → ①②③ → 发指令」 | `FishInfo` 进，`FishCtrlInfo` 出 |

官方「规则 → 代码」对照（教学页原文）：

| 比赛规则 | 官方代码里的对应含义 |
| --- | --- |
| 从 (-1250,0,340) 出发，终点 (1250,0,340) | 节点表首点 (-1250,0,0,0)、末点 (1250,0,0,0)；采样点里的 340 就是深度 |
| 绕位置 1 障碍 (-750,0) 转 1 圈 | 节点 (-750, 0, 220, 1)：圆心在位置 1 上，`circles=1` 即绕 1 圈 |
| S 形穿过中场（经过位置 2 附近） | 节点 (-150, 200, 200) 与 (150, 200, 200)：两个半径 200 的圆弧节点把路线拧成 S 形 |
| 绕位置 3、4 之间区域转 2 圈 | 节点 (750, 0, 220, 2)：圆心在位置 3、4 之间，半径 220 的圆把两根柱子圈在绕行范围内 |
| 按顺序完成任务，不能抄捷径 | `lookahead_distance=80`：只在眼前 80mm 内选目标点 |
| 尾舵关节 ±80° 可控 | `max_tail_angle=80`；实际修正量限幅 ±65°，给 sin 摆动留 ±15° 余量 |
| 胸鳍推力上限 50 | `max_wing_force=50`，`wing_force=10` 是巡航用的比例系数 |
| 60 秒时限，用时越短名次越高 | 主循环每 1/60 秒跑一帧；提速靠优化路径长度与控制平滑度 |
| 用 Python 3.7 写策略 | 4 个 `.py` 组成，`main.py` 是入口，经 `mycue` 库与平台通信 |

**本项目采用单文件 `test.py`**，把上述四段职责收敛在同一个文件里，用等价结构实现：

| 官方文件 | `test.py` 中的对应部分 |
| --- | --- |
| `path_generate.py` | `PathBuilder`（直线/圆弧/贝塞尔）、`generate_sections()`、`PathSection` |
| `find_point.py` | `PathTracker`（单调前进的纯追踪 + 自适应前瞻）、`_guidance()` |
| `control.py` | `RaceController`、`PIDController`、`DepthController`、`_motion_profile()`、`_entry_guard()` |
| `main.py` | `main()`：`mycue.init` / `publisher` / `subscriber` + 主循环与看门狗 |

单文件的好处是调试期改一处、跑一次，平台调试模式下可直接加载；
**若官网最终只收 4 个 `.py`，需要把 `test.py` 拆包**（见第 8 节）。

---

## 5. 通信接口（`cue` 库）

```python
import cue as mycue
mycue.init(TEAM_NAME)                                # 队名 → 唯一通信 ID
publisher  = mycue.publisher(mycue.FishCtrlInfo)     # 发控制量
subscriber = mycue.subscriber(mycue.FishInfo, lcb)   # 收鱼状态，lcb 是回调
```

数据结构（`FishProjectMsg.py`）：

```python
FishInfo:      pos(x,y,z), rot(Roll,Pitch,Yaw), forward(x,y,z)
FishCtrlInfo:  wing_target_angel_left, wing_target_angel_right,
               wing_force_left, wing_force_right, tail_target_angel
```

字段映射（`test.py` 的 `publish()`）：

| 平台字段 | 本代码 | 说明 |
| --- | --- | --- |
| `tail_target_angel` | `command.tail` | 尾舵角，限幅 ±80 |
| `wing_force_left/right` | `command.left/right` | 推力，限幅 ±50 |
| `wing_target_angel_left/right` | `command.left_angle/right_angle` | 胸鳍倾角 |

两个必须注意的坑（已在代码里处理，勿改回）：

1. **必须先建 publisher，再注册 subscriber**。否则 DDS 回调可能在发布者就绪前触发，
   产生启动竞态。
2. **胸鳍角度语义**：平台沿胸鳍 `GetUpVector` 施力。`MakeRotator(0, angle, 0)` 下
   `angle=0` 是**向上推**，`angle=-90` 才是**沿鱼身向前推**。
   因此不能把胸鳍角度限制在 ±25°，也不能用「向上顶」的方式去控制深度。

---

## 6. 运行与自测

内嵌解释器路径：`WindowsNoEditor\Fish427\Binaries\Win64\python.exe`
（`test.py` 的 `main()` 会自行把它插入 `sys.path`，不要求写入全局 PATH。）

```powershell
# 逻辑自测：不导入 cue、不连接平台，纯几何/控制校验
& '.\WindowsNoEditor\Fish427\Binaries\Win64\python.exe' -B test.py --self-test
& '.\WindowsNoEditor\Fish427\Binaries\Win64\python.exe' -B test.py --stable --self-test

# 实跑（需先在仿真平台勾选「调试模式」并开始仿真）
& '.\WindowsNoEditor\Fish427\Binaries\Win64\python.exe' -B test.py
& '.\WindowsNoEditor\Fish427\Binaries\Win64\python.exe' -B test.py --debug   # 每秒打印状态
```

命令行开关：

| 开关 | 作用 |
| --- | --- |
| `--self-test` | 跑 38 项单元自测后退出 |
| `--stable` | 使用保守档路线/速度参数（`USE_STABLE_PROFILE`） |
| `--debug` | 每秒打印一行状态，并把 DDS 发送改为 `publisher.publish()`（会逐帧 print） |

**自测只覆盖几何与控制逻辑，不能代替比赛成绩验收。**

实跑时的控制台输出（按阶段切换时打印，是排查耗时的主要依据）：

```
[race] {elapsed}s stage={阶段} pos=... speed={实际}/{目标} yaw_error=... z_target={锁定高度}
       vz=... pitch=... wings=(左,右) laps={各段圈数} stage_seconds={各段耗时}
```

**优化耗时请优先看 `stage_seconds`**，它按 `approach_1 / orbit_1 / slalom_2 / orbit_3 / finish`
分段给出实际用时，能直接定位是哪一段慢。

官方成绩记录在 `WindowsNoEditor\Scripts\project1.csv`，格式为
`文件名,成绩,日期,时间`（超时或违规记「无效成绩」）。**该文件由裁判/平台写入，
策略代码不得改写它**。

---

## 7. 代码结构与关键常量

阶段划分（`generate_sections()`），每段独立跟踪，前瞻点不会跨段：

| 阶段 | 内容 | 圈数 | 绕行圆心 |
| --- | --- | --- | --- |
| `approach_1` | 起点 → 障碍 1 南侧入圈点 | 0 | — |
| `orbit_1` | 绕障碍 1 | **1**（逆时针） | `OBS1` |
| `slalom_2` | 对正 → 穿缝 → **出缝即内切**（见 7.1）→ 障碍 3 入圈点 | 0 | — |
| `orbit_3` | 绕障碍 3 | **2**（逆时针） | `OBS3` |
| `finish` | 出圈 → 终点 | 0 | — |

圈数由 `OrbitProgress` 用**实际轨迹累计的有符号角度**判定，不靠路径索引猜，
逆行会抵消、瞬移不计数——保证「1 圈就是 1 圈、2 圈就是 2 圈」。

关键常量（`test.py` 顶部）：

| 常量 | 值 | 含义 |
| --- | --- | --- |
| `START_Z` | 340.0 | 鱼的起点高度（官方起点坐标 z） |
| `HOLD_Z` | 340.0 | **全程锁定高度**，等于 `START_Z`（见 7.3） |
| `LOOP_R` | 200.0 / 210.0 | 绕行半径（快档 / stable 档） |
| `MAX_THRUST` | 50.0 | 推力上限（平台硬限制，不可放宽） |
| `MAX_TAIL` | 80.0 | 尾舵限幅（平台硬限制，不可放宽） |
| `WING_NEUTRAL` | -90.0 | 胸鳍「沿鱼身向前」的基准角 |
| `MAX_WING_TILT` | 85.0 | 相对基准的最大倾角 |
| `SPEED_GAIN` | 1.16 | **整体提速系数**（见下） |
| `CURVE_ARM_RATIO` | 0.85 | 出缝后内切贝塞尔的控制臂比例（见第 7.1 节） |
| `SAMPLE_STEP` | 8.0 | 路径采样步长 |

### 7.1 出缝后立刻内切（v6）

旧版 `slalom_2` 是「先向北偏到 `y=+35`、再俯冲到南侧」的两段贝塞尔，
转折点被推到第三柱西侧（`x≈450`）才出现，所以看起来像「到第三柱才开始向里走」。
v6 换成**一条**与两端切向都连续的三次贝塞尔：

```
控制点: (190, 0) → (190+arm, 0) → (750-arm, -LOOP_R) → (750, -LOOP_R)
        arm = CURVE_ARM_RATIO * LOOP_R
```

两端切向都是 `+x`，分别与「直缝」和「`orbit_3` 圆弧起点（270°）」相切，
因此段与段之间没有折角。实测（快档）：

| 指标 | 值 |
| --- | --- |
| 出缝后首次 `y < -5` | `x=242`，即出缝后 **52mm** |
| 出缝后首次 `y < -20` | `x=297`，即出缝后 **107mm** |
| 到达第三柱西侧 `x=550` 时 | `y=-140`，横移已完成 **70%** |
| 最大曲率 | `0.0045`（`1/222`），**低于**绕圈曲率 `1/200` |
| 路径长度 | 1583mm（比旧版 1631mm **更短**） |
| `entry_guard` 触发 | **0 次**（旧版会在第三柱前触发，把推力钳到 32.5 而拖慢入圈） |

出缝后全程 `y` 单调递减，不存在「先向北再向南」的回头弯。
对应的自测是 `test_v6_immediate_inward_turn_after_gap`，它直接断言上面三条验收标准。

### 7.2 提速系数 `SPEED_GAIN`

做法是把**推进量按同一系数放大**：尾摆频率、尾摆幅值、巡航/转弯/穿缝目标速度、
转弯加速度预算、速度环增益、穿缝减速斜率、各类保守推力上限。
**平台的硬限幅（推力 50、尾角 ±80）、安全间隙 55、圈数判定一律不改。**

沿革：v5.1 按用户要求 +8%（`1.08`）；v6 用户要求「再快一些」，提到 `1.16`。

三个必须记住的实现细节：

1. **快档的推力恒被钳在 50。** `THRUST_BASE` 本身就是 `50 = MAX_THRUST`，
   所以 `CRUISE_SPEED` / `TURN_SPEED` 这些**目标速度数值本身不改变实际推力**，
   它们只影响速度环的饱和方向。**真正决定快慢的是尾摆频率与幅值**
   （手册：尾摆是主要推进方式，摆动频率与动力成正比）。
   所以任何"提速"都必须落到 `TAIL_*_HZ` / `TAIL_AMPLITUDE_*` 上，
   只调目标速度是无效的。
2. **绕圈限速公式是 `sqrt(TURN_ACCEL_BUDGET / 曲率)`**。若预算只乘一次系数，
   绕圈速度只涨 `sqrt(k)`；所以预算按系数的**平方**放大，绕圈速度才真到 `k` 倍。
3. **要回到提速前的速度，只改 `SPEED_GAIN = 1.0`**，不需要动其它任何一行。

推进量放大后要复查一件事：绕圈时尾摆幅值 `TAIL_AMPLITUDE_TURN` 会不会被
`MAX_TAIL - |correction|` 裁掉。一旦被裁，提速会被静默抵消。当前规划 20.88°、
允许 69.0°（`MAX_TAIL` 80 减去偏航修正），未触发裁剪；
**若今后调大摆幅或 PID 增益，必须重新验算这一点。**

### 7.3 高度锁定为起点高度（v7）

**目标高度 = 鱼的起点高度**，全程不变：`HOLD_Z = START_Z = 340.0`。

旧版的目标高度是 `TARGET_Z = 750.0`（水池中层，1500 高的中间）。而起点在
`(-1250, 0, 340)`——**画面下半部分**。所以旧版开局会先爬升到中层，
既多花时间，又多一次「调整到中间高度」的姿态动作。

v7 直接把目标高度换成起点高度，于是：

- 开赛瞬间 `height_error = HOLD_Z - z = 0`，垂直速度目标与俯仰目标**恒为 0**；
- 不存在「先移动到画面中间」，也不存在「以画面中间高度为过渡高度」；
- 绕障、穿缝、绕圈、冲线**全程保持同一高度**，不再有高度调整动作。

验证（`test_v7_holds_start_height_all_the_way`）：
沿整条名义路线（含 1 圈 + 穿缝 + 2 圈）逐点喂入 `z = START_Z`，
断言 `reference_vz` 与 `desired_pitch` 的**非零采样点数为 0**，且能正常冲线。

> 注意：控制律本身没有被削弱。若因外力（碰撞等）真的偏离了高度，
> `DepthController` 仍会正常把它拉回来——我验算过，从 `z=750` 起步时
> 会立即产生 `ref_vz<0`、`pitch<0` 的下降指令。所以「全程不变」指的是
> 在正常执行下不主动改变高度，而不是把高度控制关掉。

想要改回中层路线，把 `HOLD_Z` 改成 `750.0` 即可（`START_Z` 保持 340 不动）。

---

## 8. 工作约定（本项目专属）

1. **只改 `test.py`。** 其它文件（含 `WindowsNoEditor/` 下的任何东西）一律不动，
   尤其不要改 `project1.csv`（裁判成绩）和 `cue.py`（平台通信库）。
2. **不要用 `computer-use` 去操作仿真界面。** 用户自己在仿真器里跑，
   AI 只负责改代码；改完通知用户即可。
3. **不要调用 `create` 技能。** 用户明确反感它的阅读开销，直接改文件。
4. **改完必须跑自测**，两档都要过：

   ```powershell
   & '.\WindowsNoEditor\Fish427\Binaries\Win64\python.exe' -B test.py --self-test
   & '.\WindowsNoEditor\Fish427\Binaries\Win64\python.exe' -B test.py --stable --self-test
   ```

   自测失败就是真失败，不要用「逻辑上应该没问题」绕过。
5. **自测通过 ≠ 成绩达标。** 汇报时必须说清楚「未实测，不能保证耗时」，
   并请用户回传成绩与控制台 `stage_seconds`。
6. **汇报用中文**，先说结论，再给依据，不要堆砌过程。
7. **改完提交并推送**到 `https://github.com/ACM555/Fish.git`：

   ```powershell
   git add -A
   git commit -m "..."
   git push origin HEAD
   ```

   `git` 需要写 `.git`，在沙箱内会被拒绝；用非沙箱模式运行（会弹授权）。
   `core.autocrlf` 已设为 `false`——`test.py` 依赖精确行匹配打补丁，
   行尾被自动转换会导致后续改动定位失败，**不要改回 `true`**。

---

## 9. 待确认事项（已知分歧，务必优先核实）

### 9.1 立柱布局：官方教学页 与 `test.py` 不一致

| 来源 | 位置 2 | 位置 3 / 4 | 绕 2 圈的圆心 |
| --- | --- | --- | --- |
| 官方教学页（规则图解） | `(0, 0)` 单柱 | `(750, 175)` / `(750, -175)` 双柱 | `(750, 0)`，半径 220 |
| `test.py` 当前实现 | `(0, ±175)` **双柱**，净空 y∈(-75,75) | `(750, 0)` 单柱 | `(750, 0)`，半径 200 |

两者**把双柱和单柱的位置对调了**（关于 x=0 镜像）。
绕 2 圈的圆心 `(750, 0)` 两边一致，所以「绕 2 圈」这一条不受影响；
但「S 形穿缝」这一条会走错地方：`test.py` 认为窄缝在 x=0，
官方规则图解认为窄缝在 x=750。

**几何验算（已做）**：按官方教学页的数字，绕 `(750, 0)` 半径 200 或 220 转圈，
路线会**正好穿过** `(750, ±175)` 的柱子，最小间隙 **0.0 mm**——即官方教学页的
「双柱位置 + 圆心/半径」自身就是矛盾的。而 `test.py` 的模型（单柱在 `(750,0)`）
在半径 200 时最小间隙 **58.6 mm**、半径 210 时 **68.6 mm**，与自测要求的 ≥55 mm 自洽。

**实跑佐证**：`project1.csv` 中 `test.py` 在 2026-09-24 多次取得 18~22s 的有效成绩。
若真实地图是双柱在 `(750, ±175)`，`test.py` 绕 `(750,0)` 半径 200 的圆会**正好穿柱**，
要连续转 2 圈、且 `orbit_3` 段只花约 4.3s，几乎不可能做到。
（注意：撞柱未必被判「无效成绩」——平台主要对超时记无效，
所以这条只是**旁证**，不是铁证。）

**结论（推断，非官方确认）**：真实地图更可能是 `test.py` 的模型，
即**中场是双柱窄缝、后方 `(750,0)` 是单柱**；官方教学页的图解/参数存在误差。
但「能完赛」不等于「走对了缝」，因此本条仍建议人工确认一次。

**如何核实（用户在仿真器里做）**：从起点朝终点看，确认中场是一根柱还是两根柱；
或看 `fish_debug_log.csv` 里绕圈段的实际圆心与半径。
**在核实之前，不要基于某一方的假设去大改路径几何。**

### 9.2 提交形态：单文件 vs 4 文件

官方课程说明「提交内容：策略文件（4 个 `.py`）上传比赛官网」，且示范代码为
`path_generate.py` / `find_point.py` / `control.py` / `main.py`。
本项目目前是单文件 `test.py`，仿真平台调试模式下可直接加载。
**若官网最终要求 4 个文件，需要拆包**；拆包时按第 4 节的对应关系切分，
保持 `main.py` 为入口、`import cue` 只出现在入口文件。

### 9.3 官方示范代码的参考参数

教学页披露的官方参数，拆包或对照时可直接复用：

| 参数 | 值 | 说明 |
| --- | --- | --- |
| `lookahead_distance` | 80 | 只在眼前 80mm 内选目标点，防止抄捷径 |
| `max_tail_angle` | 80 | 尾舵硬限幅；实际修正限 ±65°，留 ±15° 给 sin 摆动 |
| `max_wing_force` | 50 | 推力硬限幅 |
| `wing_force` | 10 | 日常巡航用的比例系数 |
| 路径节点 | `(x, y, z, circles)` | 例：`(-750, 0, 220, 1)` 绕 1 圈、`(750, 0, 220, 2)` 绕 2 圈 |
| 圆弧半径 | 200 / 220 | S 形用 200；绕 2 圈用 220（把两根柱子圈进绕行范围） |
| 采样点数 | 694 | 官方 `path_generate.py` 的 Dubins 路径采样数 |

---

## 10. 术语与基础概念（读代码前先懂这 6 条）

1. **坐标系与弧度**：场地中心是原点 `(0,0)`，x 轴指向终点，单位 mm。
   代码里角度多用弧度：一圈 = `2π ≈ 6.28`，180° = `π`。换算即 `/π*180`。
2. **回调函数**：把 `lcb` 函数「登记」给仿真平台，平台每帧拿到鱼的新状态就主动调用它。
   不用管什么时候打，只要写好「接起电话后做什么」。
3. **API 接口**：约定好的「插座规格」。`cue` 库规定订阅用 `subscriber`、
   发送用 `publisher`、控制量装进 `FishCtrlInfo`。按规格插上去就能用。
4. **sin 函数为什么能让鱼尾摆动**：sin 的图像是 ±1 之间平滑往复的波浪。
   令尾角 = `幅值 × sin(相位)`，尾巴就会在 ±幅值 之间平滑左右摆——这就是仿生。
5. **为什么推力不能超过 50**：比赛规则给推进器设了功率上限，防止大家无脑堆推力、
   把比赛变成「拼电机」。所以优化方向是**让路径更短、转向更稳**，用脑子赢而不是用马力赢。
6. **相位累加器**：`test.py` 里的 `self.phase` 是计数器，每帧加
   `2π × 频率 × dt`，相当于鱼尾摆动的「节拍器」。因为它持续累加，
   `sin(phase)` 才会随时间连续变化，而不是每帧从头开始。

---

## 11. 快速上手清单

接手本项目的第一步：

1. 读本文件第 2、3 节，弄清规则与场地。
2. 跑一遍自测，确认基线是绿的。
3. 读 `test.py` 顶部 40 行注释（含 v5/v5.1/v6/v7 的设计说明与历史坑）。
4. 要动速度 → 只调 `SPEED_GAIN` 或对应的 `*_SPEED` / `TAIL_*` 常量，
   不要去放宽 `MAX_THRUST` / `MAX_TAIL`（那是平台硬限幅，改了也无效）。
5. 要动路线 → 改 `generate_sections()` 里的 `PathBuilder` 段，
   然后跑 `--self-test`；`test_geometry` 会校验**所有采样点**离每根柱子 ≥55mm。
6. 改完提交并推送，然后请用户实测。

**容易踩的两个方向性问题**：高度目标是 `HOLD_Z`（= 起点高度 340），
**不是**水池中层 750；立柱布局以第 9.1 节的推断为准，
真实地图待人工确认前不要基于任一假设大改路径几何。
